# Loaded on top of .env by `pnpm run start` (repo root) / `start:local` here —
# only overrides PORT, so this isolated "production" backend runs on a
# different port than the dev server (6501) and doesn't collide with it.
# No secrets — safe to commit, same as frontend/.env.production.
PORT=6502
